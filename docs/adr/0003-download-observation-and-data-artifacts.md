# 以下载完成观察和可分析数据工件处理非 HTML 导航

WebRetriever 对 2xx 顶层非 HTML 文档和显式浏览器下载执行下载完成观察：最多等待十分钟，完成后才形成首次观察；超时或失败只结束该工件并让 Agent 改走其他一方路径。JSON、CSV、TSV、XLSX 与 ZIP 内这些格式在保留原件和来源后归一化为可分析数据工件，并通过既有受限数据分析入口按需分析；这是为了避免把大型一方结构化响应错误地当作网页截图或截断 DOM 文本。
