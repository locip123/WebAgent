# 在分析适配层派生合法标识符

下载工件保留源标识符以维持审计性；数据分析适配层则为 PandasAI/SQL 派生不超过 64 个字符的稳定合法分析标识符，并将双向映射写入分析结果的溯源信息。选择此边界而非原地重写 manifest，是为了兼容任意上游字段名，同时不破坏已有工件契约。

## Considered Options

- 在下载工件阶段重写表名和列名。
- 保持原始名称，并在分析适配层使用可追溯的派生标识符。
