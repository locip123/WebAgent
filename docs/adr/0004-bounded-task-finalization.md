# 以有界收尾保证任务终态落盘

WebRetriever 的主体 Agent 到达配置任务时限后先确定其终态，再给予运行时最多 60 秒的收尾窗口；因此单次运行的绝对上限为主体时限加 60 秒。收尾开始即取消未完成的浏览器后台任务，只保留已完成证据；若仍无法结束，则在 `result.json` 写入独立收尾诊断而不覆盖主体终态。这以少量超时边缘证据换取任务锁释放、结果可审计和 worker 可继续处理后续任务。
