# 首轮路径审查使用状态相关的输出 schema

首轮路径审查把普通的 `add`/`update` 路径操作 schema 替换为仅含根 `"1"` 直接子路径 `add` 的 schema；常规浏览器行动必须创建至少一条路径，唯一例外是带完整答案和证据的首屏 `finish(success=true)`。首轮批次仍由执行器拒绝任何 `update` 或非根子路径，首轮成功后恢复通用 schema。相比只强化提示词或只做事后校验，这会在真实 provider 输出约束中移除模型反复选择的错误分支，同时保留对旁路和直接完成场景的防御与语义。
