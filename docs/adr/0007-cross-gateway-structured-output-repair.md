# 跨网关组的结构化输出修复

当模型返回的内容不能校验为当前决策信封时，agent 在同一浏览器步骤内发起带安全格式诊断的修复请求，并排除产生该内容的整个规范化 API 基址服务组。合法 JSON 的路径语义错误继续使用既有修复流程，不排除服务组；每一步最多三次连续结构化输出错误，成功进入下一浏览器步骤后服务组恢复可选。

## Considered Options

仅更换服务别名会重试同一网关，路由器隐式换服务又不会携带修复诊断；两者都不能可靠纠正结构化输出。
