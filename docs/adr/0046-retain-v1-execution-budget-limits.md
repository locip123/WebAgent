# Retain the existing execution budgets for desktop v1

Desktop v1 retains the Runner's limits of 100 steps per task, 180 seconds per model wait, and 8 concurrent tasks.  They are product safety and regression-compatibility limits rather than competition rules; changing any value later requires an explicit versioned decision backed by operational evidence.
