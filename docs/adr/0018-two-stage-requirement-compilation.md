# 浏览前独立编译并审查任务要求

WebRetriever 在浏览开始前由 Requirement Compiler 将权威自然语言任务编译为原子完成条件，再由不接触候选答案或浏览轨迹的 Requirement Auditor 对照原任务检查遗漏、错误合并和额外要求；审查通过后执行器才冻结账本并启动浏览 Agent。答案格式由浏览 Agent 根据任务自由表达，不在账本中预先定义。相比由浏览 Agent 首轮拆解或只用确定性关键词规则，两阶段编译增加了模型调用成本，但隔离了“根据容易找到的答案反向缩小任务”的动机，并能覆盖任意自然语言约束。
