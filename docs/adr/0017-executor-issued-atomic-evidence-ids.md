# 执行器签发原子 Evidence ID

WebRetriever 只把执行器从任务浏览轨迹中登记、且具有精确来源定位的原子片段视为可引用证据，并由任务内证据注册表签发不可变 Evidence ID；浏览 Agent 只能在候选声明中引用已有 ID，不能编写证据文本或用整页、步骤编号和 URL 充当证明。这个选择需要为网页、DOM、网络、工件、图表和派生计算维护不同定位器，但能让完成门禁解析并核验每个引用，避免自然语言“来源说明”掩盖无依据答案。
