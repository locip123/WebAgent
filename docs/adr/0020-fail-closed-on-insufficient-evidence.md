# 预算耗尽时对证据不足关闭成功路径

完成门禁拒绝答案候选时，WebRetriever 将逐要求验证缺口反馈给浏览 Agent 并在剩余时间和浏览步骤内继续取证；答案候选与验证调用不占浏览动作步数，相同候选及证据组合不会重复调用验证器。若预算耗尽后仍有强制要求未获支持，执行器写入 `FAIL_INSUFFICIENT_EVIDENCE` 并保留最后候选和验证结果，绝不因时间紧迫降级为 `SUCCESS` 或误记为 `FAIL_MODEL`。该策略可能降低表面成功率，但让成功精度优先于无证据猜测。
