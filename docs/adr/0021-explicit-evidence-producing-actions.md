# 只有显式取证动作可以登记答案证据

WebRetriever 只允许显式取证动作向证据注册表写入原子证据：文本查找、元素读取、网络字段提取、图表数据发现、数据分析、带已有 Evidence ID 输入的计算，以及新增的视觉证据捕获；点击、滚动、等待、URL、页面观察、模型记忆和路径进展都不能直接支撑最终声明。即使答案已在首屏可见，浏览 Agent 也必须先执行取证动作。这个约束增加一次显式读取成本，并要求为图表和 PDF 保存区域截图，但阻止无法定位的“刚才看到过”被升级为证明。
