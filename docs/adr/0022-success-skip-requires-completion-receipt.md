# SUCCESS 跳过需要有效完成回执

新版本 WebRetriever 只有在 `SUCCESS` 结果同时携带执行器完成门禁签发且摘要匹配的完成回执时才跳过任务；旧版无回执成功结果保持可读且不被自动改写，但任务再次进入运行范围时必须重新执行。结果外层继续保留 `agent_answer` 等兼容字段，账本、结构化证据和验证记录作为新增工件持久化。这个迁移会使复用旧输出目录时重跑更多任务，却能防止历史模型自报成功绕开新门禁。
