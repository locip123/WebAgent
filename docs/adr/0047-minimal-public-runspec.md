# Keep the desktop RunSpec minimal

Desktop v1 exposes only input and output selection, credential `profile_id`, local-browser headed mode, bounded execution limits, and task selection.  Prompt language, structured prompt logging, rerun behavior, experiments, and custom user-agent settings remain fixed safe defaults outside the public API until a separately versioned need justifies exposing them.
