# Keep user task artifacts outside automatic retention

Automatic retention applies only to control-plane records and event journals.  Task artifacts in the user-selected output directory, including results, captures, trajectories, and model records, are never deleted automatically; any removal must be an explicit future user action.
