{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main local WebRetriever window can use only the selected native dialog and narrow application commands.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
