# WebRetriever 执行协议

此上下文定义 WebRetriever 在网页探索和模型调用期间使用的核心语言。它既区分执行器拥有的任务锚点与模型提出的可探索路线，也界定并发模型服务的恢复与调度边界。

## 提示语言边界

**模型面对文本（Model-facing Text）**：
运行期间由系统发送给模型的自有指令、契约、状态说明和修复反馈；其语言可以统一调整，但网页原文、权威任务原文和用于识别多语言网页内容的匹配词不属于模型面对文本。
_Avoid_: 全部源码字符串、网页语言识别词、日志和开发文档

**外部来源文本（External Source Text）**：
来自权威任务、网页、文档、下载工件或浏览器取证结果的原始内容；为保留语义、标签和证据可追溯性，它按来源语言透传给模型。
_Avoid_: 系统指令、运行时修复提示、自动翻译文本

**控制性叙述（Control Narrative）**：
模型为执行协议生成的决策摘要、路径位置、路径策略和路径进展；新叙述统一使用英文，并以 `"none"` 表示没有新进展，执行器可在内部兼容旧哨兵 `"无"`。
_Avoid_: 最终任务答案、外部来源文本、页面标签

**任务答案（Task Answer）**：
提交给评测方的任务结果内容；它遵循权威任务要求并保留必要的原始名称、文字和格式，不受控制性叙述的英文约束。
_Avoid_: 控制性叙述、默认英文翻译、决策摘要

## 任务完成判定

**任务要求账本**：
执行器根据权威任务持有并冻结的原子完成条件集合；浏览 Agent 只能为条件提交候选值、履约声明和证据引用，不能删除、改写或自行豁免条件。
_Avoid_: 探索路径树、模型检查清单、可变计划

**任务要求编译器（Requirement Compiler）**：
浏览开始前只读取权威任务、并将其转换为原子完成条件的独立模型角色；它不接触候选答案或浏览轨迹，也不规定答案格式。
_Avoid_: 浏览 Agent 计划、提示词中的工作方法、关键词解析器

**任务要求审查器（Requirement Auditor）**：
独立对照权威任务与编译结果，检查遗漏、错误合并和额外要求的模型角色；只有其审查通过，执行器才能冻结任务要求账本并启动浏览。
_Avoid_: 浏览 Agent 自检、完成验证器、格式校验器

**账本修订候选**：
浏览 Agent 对任务要求拆解提出的显式修订请求；它在执行器或独立审查器批准前不改变任务要求账本。
_Avoid_: Agent 自行修改要求、隐式缩小任务范围

**原子证据**：
执行器从当前任务的浏览轨迹中登记、具有精确来源定位的最小可审计事实片段；网页文本区间、DOM 元素快照、网络 JSON 值、工件行列、图表区域或带输入溯源的计算结果都可成为不同类型的原子证据。
_Avoid_: 整页泛化引用、模型来源说明、未经定位的动作输出

**证据标识符（Evidence ID）**：
执行器为原子证据签发的不可变任务内引用，解析后包含证据类型、来源定位和完整性信息；浏览 Agent 只能引用已登记标识符，不能创建、修改或用自然语言代替它。
_Avoid_: 模型生成的证据文本、步骤编号、URL 引用

**证据注册表**：
执行器持有的 Evidence ID 到原子证据及其来源定位的任务内映射，是最终声明可引用证据的唯一命名空间。
_Avoid_: Agent 记忆、提示词历史、自由文本 evidence 列表

**取证动作**：
执行器明确允许向证据注册表写入原子证据的只读动作；文本查找、元素读取、网络字段提取、图表数据发现、数据分析、带输入溯源的计算和视觉区域捕获属于该集合，普通观察、导航和路径维护不属于。
_Avoid_: 任意成功动作、页面访问记录、路径进展

**视觉证据捕获**：
从当前可信截图中选择明确区域并登记其页面快照、像素边界和图像摘要的取证动作，用于无法通过 DOM 或结构化数据表达的带标签图表及文档图像。
_Avoid_: 模型对整张截图的描述、无坐标截图、按几何估算未标注数值

**答案候选**：
浏览 Agent 提交给完成门禁的非终态答案及逐要求声明；它可以请求结束搜索，但本身不携带或产生成功状态。
_Avoid_: 最终答案、`finish(success=true)`、模型宣布成功

**答案条目**：
答案候选中可单独呈现和校验的一项答案值；它可带标签和单位，但始终以值为核心。
_Avoid_: 最终答案、证据、要求声明

**答案声明**：
答案候选中针对一个冻结任务要求的陈述，并只引用已登记的证据标识符；同一候选对同一要求只能有一项声明。
_Avoid_: 原子证据、完成验证结论、自由文本来源说明

**完成验证器（Completion Verifier）**：
独立读取权威任务、冻结账本、答案候选和已解析原子证据，并为每个强制要求判定 `entailed`、`contradicted`、`insufficient` 或 `wrong_scope` 的模型角色；它不浏览网页，也不拥有任务终态。
_Avoid_: 浏览 Agent 自检、任务要求审查器、执行器确定性校验

**完成门禁**：
执行器对答案候选依次执行证据引用完整性和独立逐要求验证的边界；只有全部强制要求获得支持时，它才写入成功状态，否则返回具体取证缺口继续任务。
_Avoid_: 提示词成功规则、非空答案检查、模型置信度

**执行器成功状态**：
只能由完成门禁签发的 `SUCCESS` 任务终态；浏览 Agent 和完成验证器均不能在结构化输出中请求或设置它。
_Avoid_: Agent 自报成功、答案候选、路径成功状态

**完成回执**：
完成门禁接受答案候选后签发的持久化记录，绑定协议版本、冻结账本、候选答案、证据集合与接受判定的摘要；只有携带可验证完成回执的成功结果才具有任务跳过资格。
_Avoid_: 模型成功字段、自由文本验证说明、旧结果状态

**旧版未经验证成功**：
状态为 `SUCCESS` 但不包含有效完成回执的历史结果；它可继续读取和比较，但任务再次进入运行范围时不能触发成功跳过。
_Avoid_: 已验证成功、损坏结果、自动迁移结果

**验证缺口**：
完成门禁拒绝答案候选后返回给浏览 Agent 的逐要求可操作诊断，说明需要补充、纠正或换作用域的具体证据；它恢复取证而不结束任务。
_Avoid_: 泛化的“请重试”、模型故障、最终失败原因

**证据不足终态**：
任务期限或浏览步骤预算耗尽时，仍有强制要求未获支持所产生的 `FAIL_INSUFFICIENT_EVIDENCE`；它保留最后答案候选和逐要求验证结果，但绝不授权提交答案。
_Avoid_: `SUCCESS`、`FAIL_MODEL`、Agent 主动放弃

**要求账本故障**：
任务期限内经过模型服务恢复仍无法得到编译并审查通过的任务要求账本所产生的 `FAIL_REQUIREMENT_LEDGER`；浏览任务不会在缺少冻结成功标准时启动或降级运行。
_Avoid_: 证据不足、浏览 Agent 拆解任务、`FAIL_MODEL`

**完成验证故障**：
完成验证器在任务期限内经过服务恢复仍无法返回合法逐要求判定所产生的 `FAIL_COMPLETION_VERIFICATION`；已有答案候选和证据继续保留，但不能生成完成回执。
_Avoid_: 验证缺口、证据不足、降级成功

## 数据分析工件

**数据分析未就绪**：当前任务尚未观察到由运行时注册的完整 `ready_data_dir`，因此数据分析助手不可选；`downloads/` 下的文档路径本身不构成可分析数据工件。只有运行时新观察并注册完整的 `ready_data_dir` 才能解除该状态。
_Avoid_: 不可分析工件、下载失败、可分析下载文档

**可用就绪数据目录**：当前任务中已由运行时注册、且尚未被数据分析适配层静态拒绝的 `ready_data_dir`。数据分析助手仅可针对该集合中的目录调用，模型可见集合必须完整，不得静默截断。
_Avoid_: 任意下载目录、不可分析工件、原始 ready_data_dir

**数据分析能力门控**：执行器按每轮的可用就绪数据目录决定是否向模型暴露数据分析助手；未就绪时必须同时收窄模型提示契约和严格输出 Schema，开放时则将 `data_dir` 收窄为该集合。对于绕过契约的调用，执行器在路径更新落盘前拒绝该整轮决策，并持续给出文档取证恢复状态，直至出现新的可用就绪数据目录。
_Avoid_: 仅靠提示词劝阻、下载文件即分析工件、动作失败

**分析未就绪恢复状态**：执行器实际拒绝一次无可用就绪数据目录的分析调用后写入的持续状态。它要求后续决策改用文档取证，直至新的可用目录出现；单纯未就绪只隐藏分析能力，不会主动注入该恢复状态。已就绪目录被静态拒绝时保留该次原始诊断；只有候选集合因此为空，才额外进入本状态。
_Avoid_: 普通未就绪、浏览器动作失败、单轮错误提示

**数据目录契约违约**：在存在可用就绪数据目录时，模型仍为数据分析动作给出集合外 `data_dir` 的结构化输出错误。该决策在同一步修复，不执行浏览器动作，也不提交路径更新。
_Avoid_: 数据分析未就绪、浏览器动作失败、数据分析运行失败

**门控拒绝记录**：执行器对绕过数据分析能力门控的模型决策写入的可审计轨迹项。它没有浏览器副作用，不消耗浏览器步骤或连续动作失败预算，随后在同一观察上重新决策。
_Avoid_: 已执行浏览器动作、路径进展、动作失败

**源标识符**：下载工件中保留的原始表名或列名，用于审计、溯源和还原上游数据表达；它不受 PandasAI 标识符格式约束。
_Avoid_: 清洗后的名称、SQL 名称

**分析标识符**：数据分析适配层从源标识符确定性派生、且满足分析运行时格式约束的表名或列名；它仅在分析执行中使用，并与源标识符保持可追溯映射。它最长 64 个字符；超长或无法直接转写为 ASCII 的源标识符保留可读安全前缀并携带稳定短哈希。SQL 使用分析标识符，列描述、证据和溯源保留源标识符。若规范化重名，按源标识符在工件中的出现顺序追加稳定后缀，绝不合并字段。
_Avoid_: 原始字段名、工件字段名

**不可分析工件**：在当前任务中已被数据分析适配层以静态输入错误拒绝的就绪工件。它不能再次调用数据分析助手，但仍可通过页面、导出或其他一方取证路径使用。
_Avoid_: 下载失败、浏览器 action 失败

**标识符映射**：分析标识符到源标识符的表级和列级映射。它随每个分析结果的溯源信息持久化，但不回写下载工件 manifest。
_Avoid_: 原地重命名、隐式名称转换

## 探索路径

**系统初始根路径**：
执行器在首个已观察页面创建的唯一任务锚点，标识为 `1`，固定进展以英文说明将分析起始页并添加所有可能到达任务目的地或正确页面的路线；它可持续作为当前路径，但不是可由模型修改的具体探索路线。
_Avoid_: 首轮路径、模型根节点

**根节点更新违反**：
模型请求修改系统初始根路径的协议错误；整份决策不执行，模型必须基于未改变的可信路径树重试，且该次重试使用不公开 `update` 的路径输出协议。
_Avoid_: 忽略根节点更新、部分执行

**停滞提示**：
执行器在同一具体探索路径连续五次收到值为 `"none"` 的决策摘要后发出的重新规划信号。若已有另一条未终态具体路径，模型必须切换到它并采取不同浏览器行动；否则优先新增当前可见路线，或改用不同取证方式。执行器不自行改变路径状态或标记路径失败。
_Avoid_: 自动路径失败、隐式路径更新

**模型路径树投影**：
提供给模型的路径状态表达，保留探索关系，并将系统初始根路径明确呈现为不可修改的系统锚点；根节点仅含标识、类型、不可变标记和子路径，不展示其余持久化字段。它不同于持久化路径树。
_Avoid_: 持久化路径树、模型可写路径树

**决策摘要**：
探索模式中模型用非空英文记录当前观察和即时意图的控制性叙述；没有可记录内容时可以使用 `"none"`，但首次观察到的新页面必须留下具体摘要。它不改变任何探索路径节点的进展。
_Avoid_: 路径进展、路径更新

**最近执行轨迹**：
每轮提示词中最多保留最近 12 步的紧凑执行事件数组，包含动作身份、目标、执行状态、恢复提示、简短结果、必要的路径标识、决策摘要和已应用路径变更。它受提示词预算约束并用于诊断近期动作循环；最新一步的完整执行结果另见 `Previous action outcome`。
_Avoid_: 无界完整历史、原始动作日志、最新动作完整结果

**路径进展**：
某一具体探索路径经浏览器结果或一方数据证实的阶段性成果，仅通过显式路径更新记录；计划、推测和单纯已调用的浏览器动作不构成路径进展，且不会以 `"none"` 作为更新值。
_Avoid_: 决策摘要、行动意图

**路径更新说明**：
路径操作中名为 `progress` 的英文文本值：正常更新时记录已验证的路径进展；标记失败时记录具体失败证据；标记成功时记录已验证的成功证据。它不用 `"none"` 表示未变更。
_Avoid_: 决策摘要、空更新

**具体探索路径**：
模型基于已观察的页面入口提出并追加到现有路径下的候选路线；它可被选择、更新状态或标记为失败/成功。
_Avoid_: 虚拟路径、预创建路径

**首轮路径审查**：
首次页面观察后的完整路线枚举，必须至少包含一个创建具体探索路径的 `add`，且每条首轮路径直接挂在系统根 `"1"` 下；该决策的模型输出协议仅公开 `add`，不公开 `update`，执行器也拒绝含 `update` 的首轮批次作为纵深保护。已具备完整答案和证据的首屏成功结束不创建路径，是唯一例外。创建路径不要求在同一决策立即选中子路径；首轮审查成功后的后续决策恢复通用的 `add`/`update` 协议。
_Avoid_: 根路径更新、路径预分配

**原子路线选择**：
同一决策中按顺序追加具体探索路径、选择第一个新建子路径并执行其浏览器动作的流程；只有由本轮 `add` 确定生成的路径标识可被立即选择。
_Avoid_: 预更新子路径、两阶段首轮选择

## 模型服务调度

**任务级模型恢复**：
单个任务在模型服务不可用时持续寻求可用服务直至其任务截止时间；截止只使该任务失败，不会终止同一批次中的其他任务。
_Avoid_: 全局模型故障、整批任务失败

**服务在途负载**：
某一模型服务已被分配且尚未产生响应或超时的请求数；调度器优先选择在途负载最小的服务，数值相同时轮转选择。
_Avoid_: 配置顺序、固定主服务

**模型服务调用故障**：
模型服务调用期间由提供商、网络或其 SDK 产生的任何异常；它触发任务级模型恢复，而调用方取消和本地输入或结构化输出校验失败不属于此类故障。
_Avoid_: 本地编程错误、调用方取消

**结构化输出修复故障**：
模型调用已返回内容，但该内容不能校验为本轮请求的决策信封时产生的 agent 级恢复条件。它携带安全的格式诊断，并要求下一次修复请求排除产生该内容的模型服务组；它不属于模型服务调用故障。
_Avoid_: 模型服务调用故障、路径状态校验失败、浏览器 action 失败

**结构化动作契约错误**：模型给出的动作名称或参数不符合该动作的输出契约，因而在浏览器调用前被拒绝的结构化输出修复故障。同一浏览器观察周期内按动作独立累计此类错误；通用结构化错误或其他动作的错误不会清零某个动作的累计次数。达到隐藏阈值后，该动作只在当前观察周期的后续修复请求中暂时不可选，下一次观察恢复；即使模型再次输出它也必须拒绝执行。已通过契约校验但在浏览器执行中失败的动作不属于此类错误。
_Avoid_: 浏览器动作失败、模型服务调用故障、路径状态校验失败

**服务冷却**：
模型服务调用故障后暂不接收新请求的全局等待期；连续故障使等待期指数增长，成功则立即清零，所有服务均处于冷却时任务等待最早恢复者。
_Avoid_: 永久禁用、故障服务重试风暴

**运行级模型调度器**：
在一次 CLI 运行内由全部并发任务共同使用的模型服务调度状态；独立进程拥有彼此隔离的调度器。
_Avoid_: 每任务路由器、跨进程协调器

**任务级缓存亲和**：
一个任务首次选中的模型服务是其后续模型调用的首选服务，以复用该服务可能保有的提示词缓存；亲和关系从首次调用持续至任务结束并随之清除，且受服务健康和全局负载约束。
_Avoid_: 永久绑定、调用级轮转

**软亲和负载差**：
首选模型服务的在途负载相对当前最空闲服务可容许的最大差值；差值不超过 1 时保留缓存亲和，达到 2 时让位给更空闲的服务。
_Avoid_: 无条件粘性、忽略并发压力

**故障亲和迁移**：
首选模型服务因调用故障进入冷却且替代服务成功响应后，任务将首选迁移至该替代服务；仅因负载让位的调用不会改变任务首选。
_Avoid_: 负载迁移、故障后继续偏好不可用服务

## 浏览器动作恢复

**元素索引动作**：使用当前观察生成的 `element_id` 定位页面控件的浏览器动作；索引只在其所属观察代际内有效。
_Avoid_: 坐标动作、持久元素标识符

**过期元素坐标恢复**：浏览器动作连败达到阈值时，只有最后一次失败是 `click` 的 `StaleElement` 才进入的任务内恢复策略；执行器先作恢复观察生成可信截图，再在一次恢复决策中隐藏 `click` 并提示模型优先改用像素坐标点击。恢复提示只包含执行器生成的最小可信状态，不回显旧网页文本或完整异常；恢复动作结束后的下一次正常观察才恢复 `click`。第 5 次失败照常消耗步骤，恢复额外获得一次决策和动作机会，即使它处于原 `max_steps` 的最后一步，但不延长任务绝对截止时间。恢复动作超时或返回 `error` 则终止任务，`ok`、`no_change` 或 `uncertain` 都清零连败并恢复常规探索；恢复不新增专用结构化审计记录。
_Avoid_: 通用连败恢复、结构化动作契约错误、浏览器会话恢复

## 浏览器会话恢复

**运行时未启动恢复**：任务首次观察发现浏览器运行时尚未启动时，在同一任务的原始截止时间内仅一次重新建立其起始页面并重试观察的零配额恢复；当前运行时未恢复时可升级为一次干净浏览器工作单元恢复。两级恢复均不重跑任务、不消耗模型决策轮次，恢复未完成时才进入终态。
_Avoid_: 失败任务重跑、普通观测失败、浏览器工作单元恢复

**任务页缺失续行恢复**：已启动的运行时没有活动任务页时，执行器先接管当前任务已拥有且仍安全可访问的标签页；仅在不存在该标签页时，才在原任务绝对截止时间内最多 60 秒重开起始 URL，随后才以最多 160 秒的一次干净浏览器工作单元替换并重新观察。该恢复保留已有任务轨迹和可保留的会话状态，但不重放任何已执行动作；恢复后由模型依据新页面重新决策。
_Avoid_: 任务重跑、自动重放动作、无界重试

**上下文保留重启**：任务页缺失续行恢复的第一级操作；它在现有浏览器上下文中清理失效任务页并重新创建、访问同一任务起始 URL，因此保留 cookies、登录态和权限。只有该操作失败才升级为干净工作单元恢复。
_Avoid_: 干净上下文恢复、任务重跑、跨任务复用任务页

**恢复后重新定锚**：任务页恢复成功后，执行器撤销本次观察配额、清空旧元素绑定与动作失败计数，并向模型声明页面已重建；已收集的启动 URL、网络与任务工件继续保留。模型必须基于新观察重新选择动作。
_Avoid_: 重放旧动作、沿用旧元素索引、丢弃恢复前工件

**任务页恢复耗尽隔离**：两级任务页恢复均失败后，当前任务保留 `FAIL_BROWSER_TASK_PAGE_UNAVAILABLE` 终态，但其 CDP 执行单元必须在领取下一任务前重建连接、上下文和锚点页；重建失败才熔断该执行单元。
_Avoid_: 复用不可信 runtime、失败任务重跑、立即永久退役

**浏览前连接恢复**：
任务尚未成功创建页面、访问指定起始地址或执行任何浏览器与模型行动时，对失效浏览器连接进行恢复并继续同一任务；所有恢复尝试共享该任务最初确定的绝对截止时间，可持续至截止时间但不得刷新任务预算，且不构成任务重试。
_Avoid_: 失败任务重跑、任务级重试

**浏览中断线失败**：
当前浏览器目标、上下文或连接失效，且存活标签页恢复与任务页缺失续行恢复均不能重新观察时产生的任务终态；它保留已有任务工件且不从头重跑当前任务。执行单元必须先恢复为健康会话，才能领取后续任务；恢复失败时将其熔断。
_Avoid_: 自动重跑、忽略断线继续调度

**存活标签页恢复**：
浏览器操作或观察因 Playwright 的 `TargetClosedError`、明确的目标/页面/上下文/浏览器关闭错误，或任务页指针缺失而失败时，执行器只枚举当前任务已接管且仍可访问的标签页；找到标签页则将当前任务重新绑定到其中一页并重新观察。该恢复是执行器内部的零配额恢复：旧动作不计入浏览器动作连败，也不占用步骤或模型决策轮次；模型只针对恢复后的观察作一次新决策。工作单元的锚点页只用于健康检查，不是存活标签页。没有存活标签页时，任务页指针缺失可升级为任务页缺失续行恢复；网络超时、站点脚本错误和元素过期仍是普通动作错误。
_Avoid_: 重跑当前任务、将锚点页视为任务页、将会话中断计为普通动作失败或模型轮次、误判普通动作错误

**截图生命周期故障恢复**：
观察阶段截图出现 `Target crashed`、`TargetClosedError` 或明确的页面/上下文关闭错误时，截图层立即向执行器传播会话故障，不等待普通截图重试延迟，也不在已失效目标上重复 CDP 截图；执行器按存活标签页恢复协议重新定锚。非生命周期性的截图超时或协议错误仍允许一次短延迟（3 秒）的完整截图链重试，失败后可继续无截图的文本观察。
_Avoid_: 对已崩溃目标等待 180 秒、把目标崩溃吞成无截图观察、无限截图重试

**浏览器工作单元恢复**：
一个任务以浏览中断线失败结束后，执行单元立刻丢弃本地旧 CDP 连接；领取下一任务前，它以同一 CDP 端点重连，创建干净上下文与锚点页并验证可用。恢复成功后继续调度，恢复失败或超时后熔断；恢复不占用已经失败任务的预算。
_Avoid_: 当前任务内重跑、永久退役可恢复执行单元

**浏览器执行单元熔断**：
浏览器工作单元恢复或浏览前连接恢复未能在其恢复期限内成功时，将对应浏览器执行单元移出后续任务调度；当前任务保留其已确定终态，只有在全部执行单元均不可用时，尚未领取的任务才以浏览器连接失败结束。
_Avoid_: 死亡执行单元继续领题、逐题消耗恢复期限

**未启动任务接管**：
任务尚未成功启动浏览器页面、执行浏览器动作或调用模型时，其执行单元的 CDP 连接耗尽恢复期限，该任务可由另一健康执行单元接管；若没有健康执行单元，任务才进入浏览器连接不可用终态。该接管不重放任何已发生的任务副作用。
_Avoid_: 动作后的任务重跑、静默丢题、把连接故障当作模型失败

## 运行结果诊断

**浏览器故障分类**：
任务终态中以 `FAIL_BROWSER_` 开头、并在结果工件中附带稳定 `browser_failure` 诊断对象的故障族。分类标识浏览器能力或生命周期根因，而完整错误文本仍保留在 `error`，以便同时支持聚合和逐案排查。
_Avoid_: 笼统的 FAIL_BROWSER、仅解析错误文本

**浏览器故障子类型**：
`browser_failure.subtype` 中用于描述故障族内可操作具体原因的稳定标识；它比 `category` 更细，但不替代完整 `error` 证据。任务页不可用至少区分任务页指针缺失、任务页已关闭、恢复耗尽和目标崩溃。
_Avoid_: 从错误文本临时截词、把告警文本直接当终态

**任务页恢复耗尽**：
首个动作前任务页不可用时，同一上下文重开和干净 CDP worker 替换均未恢复可观察任务页的终态原因；恢复阶段的逐项证据保存在 `browser_failure.recovery_stages`。
_Avoid_: 普通任务超时、单次重开失败、任务动作后的会话断线

**浏览器故障阶段**：
浏览器故障发生在 `startup`、`observation` 或 `action` 三个任务阶段之一；它不表示模型推理或探索路径协议阶段。
_Avoid_: 浏览器错误、任务阶段

**任务页不可用**：浏览器运行时已经启动，但当前任务没有可继续观察的活动页面。它不同于运行时从未启动，也不同于浏览器上下文或 CDP 会话整体关闭。
_Avoid_: 运行时未启动、会话丢失、无安全页面

**浏览器连接不可用**：调度器没有任何可领取的 CDP 浏览器执行单元，因此任务从未进入页面启动。它使用 `FAIL_BROWSER_CONNECTION_UNAVAILABLE`。
_Avoid_: 运行时未启动、任务页不可用、任务超时

**探索路径初始化故障**：
首个浏览器观察成功后，系统初始探索路径无法建立的协议终态。它使用 `FAIL_EXPLORATION_PATH_INITIALIZATION`，不归入浏览器故障分类。
_Avoid_: FAIL_BROWSER、浏览器观测失败

**可诊断失败任务**：
终态 `status` 以 `FAIL_` 开头，且其结果工件的 `error` 为非空文本的任务。运行器在该任务结束时输出任务序号、任务标识、终态，以及浏览器故障的 `category/subtype`（如有）和经 CDP `access_token` 脱敏后完整、原样、不截断的错误文本；成功、跳过及无错误文本的失败不产生这条诊断输出。
_Avoid_: 所有非成功任务、仅模型失败、仅结果文件错误
