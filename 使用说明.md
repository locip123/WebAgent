# WebRetriever Protocol III Agent 使用说明

本项目已增加独立的 WebRetriever Protocol III agent。入口为 `run_webretriever.py`，浏览器交互全部通过 Playwright 完成，并按比赛目录格式保存 `result.json`、`capture.json` 和双轨截图。

## 1. 环境准备

```bash
conda activate Browser-Use
cd /workspace/code/browser-use
```

当前环境使用 Python 3.12。若在新机器安装：

```bash
conda create -n Browser-Use python=3.12 -y
conda activate Browser-Use
uv pip install --python "$CONDA_PREFIX/bin/python" -e '.[core]' litellm 'httpx[socks]'
uvx playwright install chromium --with-deps
```

## 2. 配置模型

项目启动时会自动读取根目录 `.env`。Responses API 代理可使用：

```dotenv
LITELLM_MASTER_KEY=你的代理密钥
LITELLM_BASE_URL=http://127.0.0.1:4000/v1
LITELLM_MODEL=gpt-5.4
```

也可使用 `WEBRETRIEVER_API_KEY`、`WEBRETRIEVER_API_BASE`、`WEBRETRIEVER_MODEL`，或在命令行传入 `--api-key`、`--api-base`、`--model`。

不要提交 `.env`，不要在日志或聊天中输出真实密钥。比赛模型版本上限由 runner 校验；最终提交时还应以比赛 Guide 为准。

## 3. 先验证任务文件

下面的命令只校验数据，不启动浏览器、不调用模型，也不会把 `answer` 字段发送给 agent：

```bash
python run_webretriever.py \
  --input data/data/protocol3.json \
  --output outputs/validate \
  --validate-only
```

当前 `protocol3.json` 应加载 100 个任务，并显示：

```json
"ground_truth_exposed_to_agent": false
```

## 4. 本地测试

本地测试使用 Playwright Chromium，不需要评测方 CDP。测试数据中第 0、1、2 三个任务：

```bash
python run_webretriever.py \
  --input data/data/protocol3.json \
  --output outputs/protocol3_first3_local \
  --local-browser \
  --task-index 0-2 \
  --api-mode responses \
  --reasoning-effort low \
  --max-steps 100 \
  --model-timeout 180 \
  --task-timeout 300
```

`--task-index` 支持单值、逗号和闭区间，例如：

```bash
--task-index 3
--task-index 1,4-6
```

本地开发时可用 `--headed` 显示浏览器。只有本地模式允许用下面的参数覆盖失败任务：

```bash
--rerun-failed
```

`SUCCESS` 表示 agent 已产生非空答案和浏览器证据，不代表答案已经与公开数据中的参考答案离线比对。开发验收时应另外检查答案准确性。

## 5. 正式比赛运行

评测方提供 CDP URL 后，先在当前终端配置：

```bash
export CDP_URL='评测方提供的CDP地址'
```

正式运行全部 100 个任务：

```bash
python run_webretriever.py \
  --input data/data/protocol3.json \
  --output outputs/protocol3 \
  --cdp_url "$CDP_URL" \
  --api-mode responses \
  --max-steps 100 \
  --model-timeout 180
```

有多个评测浏览器时可并发运行，最多 8 个 CDP 与 8 个 worker：

```bash
python run_webretriever.py \
  --input data/data/protocol3.json \
  --output outputs/protocol3 \
  --cdp_url "$CDP_URL_1" "$CDP_URL_2" \
  --max-concurrency 2 \
  --api-mode responses
```

也可把一个或多个地址写入 `WEBRETRIEVER_CDP_URLS`，用空格或逗号分隔。

正式 CDP 模式禁止 `--rerun-failed`。每个任务只领取一次；已有终态 `result.json` 的任务会跳过，`PENDING` 任务可在重新执行同一命令时继续运行。

## 6. 自部署模型

本地 OpenAI-compatible VLM 可按端口轮询：

```bash
python run_webretriever.py \
  --input data/data/protocol3.json \
  --output outputs/protocol3_vllm \
  --local-browser \
  --vlm_ports 8000 8001 \
  --model your-model \
  --api-mode chat-completions
```

最多配置 8 个端口。自部署模型的权重与复现要求以比赛 Guide 为准。

## 7. 输出目录

每个任务目录名为 `{task_idx}_{task_id}`：

```text
outputs/protocol3/
├── 0_<task_id>/
│   ├── result.json
│   ├── capture.json
│   ├── trajectory/
│   ├── trajectory_visual/
│   └── downloads/
├── locks/
└── logs/
    ├── summary.json
    └── worker_<id>_<date>.log
```

- `result.json`：状态、动作、思考摘要、访问 URL、最终答案、证据、耗时与 token 使用量。`duration_seconds` 是 agent 循环耗时；`task_started_at`、`task_completed_at`、`task_elapsed_seconds` 记录端到端任务耗时，`task_timeout_seconds` 默认为 300 秒。达到时限会写入 `FAIL_TASK_TIMEOUT`。
- `capture.json`：Playwright 捕获的 XHR/Fetch 请求及有限响应体。
- `trajectory/`：未标注的原始截图。
- `trajectory_visual/`：带元素编号和动作标签的截图。
- `downloads/`：浏览器下载的文档；支持提取 PDF、TXT、CSV、JSON、DOCX、XLS/XLSX，以及 ZIP 内的文本/CSV。
- `logs/summary.json`：本次所选任务的状态汇总。

快速查看状态：

```bash
jq . outputs/protocol3/logs/summary.json
find outputs/protocol3 -path '*/result.json' -exec jq -r '[.task_idx,.status,.agent_answer] | @tsv' {} \;
```

常见状态包括 `PENDING`、`SUCCESS`、`FAIL`、`FAIL_MODEL`、`FAIL_MODEL_TIMEOUT`、`FAIL_BROWSER`、`FAIL_MAX_STEPS` 和 `FAIL_RUNTIME`。

## 8. 中止与恢复

在前台运行时按 `Ctrl+C`。已完成任务的产物会保留，当前任务通常保留为 `PENDING`。恢复时使用相同输入和输出目录重新运行原命令；runner 会跳过已有终态任务。

不要手工删除正式比赛输出后重跑，也不要在正式 CDP 模式使用开发参数 `--rerun-failed`。

## 9. 测试与代码检查

运行 WebRetriever 测试：

```bash
python -m pytest -q \
  tests/ci/test_webretriever_models.py \
  tests/ci/test_webretriever_browser.py \
  tests/ci/test_webretriever_agent.py \
  tests/ci/test_webretriever_runner.py \
  tests/ci/models/test_openai_responses_api.py
```

运行静态检查：

```bash
python -m ruff check browser_use/webretriever browser_use/llm/openai/chat.py
python -m pyright browser_use/webretriever browser_use/llm/openai/chat.py
```

## 10. 关键限制

- 每题最多 100 步。
- 单次模型请求最多 180 秒。
- 最多 8 个并发浏览器/worker。
- 禁止使用外部通用搜索引擎；agent 只能从任务起始网站进行站内导航和取证。
- 公开数据中的 `answer` 只可用于运行后的离线验收，绝不会进入 agent prompt。
- 正式任务不得自动重跑。
- `SUCCESS` 必须同时包含最终答案和至少一条浏览器来源证据。
