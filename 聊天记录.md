
A0 Asst(Scan→Join)
周二 11:37
大家好，欢迎来到队伍专属提交群 ，🎉
本群用于比赛期间的代码提交、环境适配沟通和正式评测，仅限当前队伍成员与组委会使用。
🔑 私有仓库授权说明：
你的 GitHub 账号已获得仓库授权，请按以下步骤完成确认：

进入 GitHub 通知栏（https://github.com/notifications) ，接受官方账号hhhhhhalf的协作邀请
确认后访问https://github.com/hhhhhhalf?tab=repositories 即可查看队伍对应私有仓库比赛内容
📄 8月13日我们将在群内发布冒烟测试说明书，并会在赛前发布正式评测的说明书，请保持关注。
⚠️请队长和成员保管好队伍信息及私有仓库内容，请勿在公共群组泄漏。

8月13日
Gan Xie
Gan Xie
昨天 13:21
你好，请问所有的代码逻辑必须放在src目录下吗

董巍
董巍
@明略科技
昨天 13:23
Gan Xie
你好，请问所有的代码逻辑必须放在src目录下吗
@Gan Xie 您好是的，agent代码逻辑放在src下面，之后scripts/run.sh作为统一的入口

Gan Xie
Gan Xie
昨天 13:33
因为我的代码逻辑比较多和复杂，我可以把scripts/run.sh作为统一入口，但代码逻辑模块分布在不同的目录下可以吗

董巍
董巍
@明略科技
昨天 13:39
Gan Xie
因为我的代码逻辑比较多和复杂，我可以把scripts/run.sh作为统一入口，但代码逻辑模块分布在不同的目录下可以吗
@Gan Xie 如果是这种情况的话，也行的。但是要确保需要的scripts/run.sh、config.json、environment.yml都是有的。还有就是模板里src.agent/main.py的多进程方案目前是按照输入的CDP_URL列表个数来确定并发进程数的，比赛期间会提供8个沙箱的CDP_URL进行8进程并发评测，这个逻辑不要改动。

Gan Xie
Gan Xie
昨天 16:03
请问云端浏览器是否以 --headless 运行，还是在 Xvfb 等虚拟显示中运行普通 headed Chrome，因为我发现无头浏览器触发风控的可能性高很多，在 Xvfb 等虚拟显示中运行普通 headed Chrome能更好避免风控

董巍邀请WR-EvalBot-05加入群聊
董巍
董巍
@明略科技
昨天 18:56
Gan Xie
请问云端浏览器是否以 --headless 运行，还是在 Xvfb 等虚拟显示中运行普通 headed Chrome，因为我发现无头浏览器触发风控的可能性高很多，在 Xvfb 等虚拟显示中运行普通 headed Chrome能更好避免风控
@Gan Xie 您好云端浏览器不是无头运行的

A0 Asst(Scan→Join)
A0 Asst(Scan→Join)
昨天 19:10
📋 WebRetriever Challenge 冒烟测试说明书

一、赛题概述
构建一个 GUI Agent，操控浏览器完成网页信息检索：导航到目标页面，提取答案写入 result.json 的 agent_answer 字段。

二、评分机制

两项指标：导航是否成功 + agent_answer 与标准答案的语义一致性
每题上限 100 步，超出计 0 分
agent_answer 为空计 0 分
三、参赛者需完成的工作

1. 配置模型 API
在 config.json 中填写模型 API 信息，支持自部署模型（公网 IP + 端口列表）或闭源 API。
⚠️ 自部署模型必须为公网可达地址，评测在云端沙箱运行，无法访问内网。

2. 自定义 Agent 逻辑
在 src/agent/ 下修改 agent.py、prompts.py、web_controller.py。框架和模型均不限。

3. 实现答案提取
模板中 agent_answer 默认留空，需参赛者自行实现。方式不限：OCR、DOM 解析、VLM 问答、网络请求捕获等。

4. 提交
推送至仓库 main 分支即可。

四、修改权限
🔒 不可修改：run.sh 参数接口、init_playwright_context 连接沙箱逻辑、输出路径格式
✅ Agent 实现逻辑可修改

五、输出格式
每题在 {output_dir}/{task_idx}_{task_id}/ 下产出，格式须与模板保持一致：


├── result.json *          ← 评分依据
├── trajectory/ *          ← 每步截图
├── capture.json *         ← 网络请求捕获
└── trajectory_visual/     ← 可视化截图（建议）
* 为必须产出
其中 result.json 的 agent_answer 字段为必填，直接影响评分。

六、多进程并行机制
传入的 CDP URL 个数即为并发进程数，此逻辑不可改动。官方默认提供 8 个浏览器沙箱，main.py 据此启动 8 个 Worker，将任务均匀分片后各自串行执行。

自部署模型：Worker 通过 api_ports 轮询分配端口，分摊推理负载
闭源 API：所有 Worker 共用 api_base
⚠️ 参赛者需确保模型 API 能支持 8 路并发请求。
断点续跑：中断后重新运行会自动跳过已完成的任务。
A0 Asst(Scan→Join)
A0 Asst(Scan→Join)
昨天 20:04
冒烟测试操作流程

选手进入专属提交群后，可先 @WR-EvalBot 发送「你是谁」，评测Bot 将介绍其具体职能与支持的指令。

冒烟测试具体流程如下：

一、上传代码至 GitHub 仓库
将最新代码 push 至比赛仓库的 main 分支。评测系统仅拉取 main 分支的最新 commit，本地未 push 的代码不会被评测。

二、提交评测
在专属提交群中 @WR-EvalBot 发送「提交代码」，系统将自动执行以下流程：
拉取代码 → 语法检查 → 环境安装 → 冒烟测试
完成后将输出冒烟测试是否通过的结果。

8月14日
Gan Xie
Gan Xie
13:34
请问方便说一下浏览器是否运行于 Xvfb、X11 或 Wayland，还有屏幕分辨率默认窗口尺寸是多少吗？

董巍
董巍
@明略科技
15:01
Gan Xie
请问方便说一下浏览器是否运行于 Xvfb、X11 或 Wayland，还有屏幕分辨率默认窗口尺寸是多少吗？
@Gan Xie 您好，浏览器沙箱环境是腾讯云沙箱，您可以去官网了解，浏览器屏幕分辨率默认窗口尺寸是1920 × 1080。

A0 Asst(Scan→Join)
A0 Asst(Scan→Join)
17:55
Agent 域名白名单收集
请提供您的 Agent 运行时实际请求的算法服务入口域名，用于加入网络白名单。

⚠️ 只需提供 Agent 直接访问的入口域名。如通过网关调用 GPT/Claude 等，只需提供网关域名。
示例：

直接调用官方 API → api.moonshot.cn
通过网关调用 → api.my-gateway.com
自部署服务 → api.my-model.com
注意：
自部署服务若部署在中国大陆，可提供 IP 地址；若部署在大陆以外，必须提供域名，不能仅提供 IP
使用多个服务请提供所有域名
请在正式提交前确认并提交，以免影响比赛运行
如有其他特殊网络访问需求，请一并说明。
📋 提交表单： https://ml.jsjform.com/f/jXbK5J